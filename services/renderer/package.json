{
  "name": "@yt/renderer",
  "version": "0.0.0",
  "private": true,
  "description": "Remotion project for the AI YouTube Factory render block (spec stage 12, Editor). Renders out/video.mp4 and the five composited thumbnail stills as a pure function of a videoSpec.json file — no AI model involved.",
  "scripts": {
    "render": "tsx src/render.ts",
    "render-thumbnails": "tsx src/render-thumbnails.ts",
    "preview": "remotion studio src/Root.tsx",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "remotion": "^4.0.0",
    "@remotion/cli": "^4.0.0",
    "@remotion/renderer": "^4.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "tsx": "^4.0.0",
    "typescript": "^5.5.0"
  }
}
